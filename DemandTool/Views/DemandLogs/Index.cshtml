@model IEnumerable<DemandTool.MVC.Models.DemandLogs>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Demand.DemandNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssignedTeam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeamStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Demand.DemandNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AssignedTeam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
